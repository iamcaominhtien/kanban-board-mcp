---
title: "Test Plan: IAM-88 and IAM-89 Markdown and Image Support"
type: test
status: review
ticket: IAM-88, IAM-89
version: 1.0.1
created: 2026-04-17
updated: 2026-04-17
authors: [GitHub Copilot (QC Agent)]
related:
  - ui/src/components/MarkdownEditor.tsx
  - ui/src/components/MarkdownRenderer.tsx
  - ui/src/components/WorkLogSection.tsx
  - ui/src/components/TestCasesSection.tsx
---

# Test Plan: IAM-88 and IAM-89 Markdown and Image Support

## Scope

This plan validates two feature areas that share the same Markdown editing and image-upload implementation:

- IAM-88: Markdown and image support for Work Log notes
- IAM-89: Markdown and image support for the Test Case Proof field

Coverage includes:

- Markdown authoring for bold, italic, lists, links, headings, quotes, and code blocks
- Markdown rendering in read/view mode through the shared renderer
- Clipboard image paste into the shared editor
- File-picker image upload from the shared editor toolbar
- Supported image formats: `png`, `jpg`, `jpeg`, `gif`, `webp`
- Persistence after save, collapse/expand, modal close, and page reload
- Error-handling and boundary behavior relevant to shared editor logic

Out of scope:

- Drag-and-drop uploads
- Image resize, crop, compression, or delete workflows
- Backend storage cleanup after referenced images are removed from content
- Browser-specific clipboard quirks outside the primary QA browser

## Implementation Notes

Observed behavior from the implementation files under test:

- `MarkdownEditor` provides toolbar insertion for markdown syntax and an `Img` file-picker action
- Clipboard paste uploads only supported `image/png`, `image/jpeg`, `image/gif`, and `image/webp` MIME types
- Unsupported file types selected outside the native file-picker filter are ignored client-side
- Uploads insert temporary markdown placeholders and replace them with returned markdown on success
- Failed uploads remove the temporary placeholder
- `MarkdownRenderer` renders markdown through `react-markdown` with `remark-gfm` and a sanitized schema that allows safe `img` tags with `http` and `https` sources
- Work Log view mode renders saved notes through `MarkdownRenderer`
- Test Case Proof uses `MarkdownEditor` in edit mode and the same markdown rendering path in view mode

## Test Approach

- Primary execution method: Playwright-based UI testing against the local app
- Setup strategy: create isolated `[TEST]` tickets and test cases so saved markdown and images can be reloaded safely
- Assertion strategy:
  - Visual: verify formatted text and images in view mode
  - Behavioral: verify paste/upload, save, collapse/expand, and reload persistence
  - Negative: verify unsupported content does not corrupt saved data or leave upload placeholders behind

## Assumptions and Preconditions

- Frontend and backend are running in the same environment
- The image upload API used by `uploadDescriptionImage` is reachable
- The tester can create or edit at least one ticket with an expandable Work Log section and a Test Cases section
- Test image fixtures are available locally for all supported formats
- Reload means either a hard browser refresh or reopening the same ticket after data has been persisted

## Test Data

| ID | Data | Purpose |
|---|---|---|
| TD-01 | `[TEST] IAM-88-89 Markdown Upload` ticket | Shared safe target for all scenarios |
| TD-02 | Work Log author `QC Tester` | Stable identity for work-log entries |
| TD-03 | Proof test case titled `[TEST] Proof markdown coverage` | Stable proof target |
| TD-04 | `tiny-diagram.png` | Baseline PNG upload |
| TD-05 | `photo.jpg` | JPG upload |
| TD-06 | `photo-alt.jpeg` | JPEG upload |
| TD-07 | `demo.gif` | GIF upload |
| TD-08 | `preview.webp` | WEBP upload |
| TD-09 | Unsupported file such as `notes.txt` or `sample.svg` | Invalid upload path |
| TD-10 | Markdown seed: `**bold**`, `*italic*`, `- item`, `[link](https://example.com)` | Base markdown formatting validation |
| TD-11 | Mixed markdown block with heading, list, quote, link, inline code, and fenced code | Rich rendering regression |
| TD-12 | Clipboard image copied from local file or screenshot tool | Paste validation |
| TD-13 | Plain-text clipboard content | Non-image paste regression |

Suggested rich markdown seed for TD-11:

````md
# Evidence

This is **bold** and this is *italic*.

- first item
- second item

> quoted note

[Reference](https://example.com)

`inline code`

```ts
console.log('proof');
```
````

## Acceptance Criteria Coverage Matrix

| Ticket | AC | Covered By |
|---|---|---|
| IAM-88 | AC1 Markdown authoring in notes | IAM88-TC-01, IAM88-TC-02 |
| IAM-88 | AC2 Worklog viewer renders markdown correctly | IAM88-TC-03, IAM88-TC-04 |
| IAM-88 | AC3 Paste images into editor | IAM88-TC-05, IAM88-TC-09 |
| IAM-88 | AC4 File-picker upload mechanism exists | IAM88-TC-06 |
| IAM-88 | AC5 Supported formats process successfully | IAM88-TC-07 |
| IAM-88 | AC6 Reload preserves markdown and images | IAM88-TC-08 |
| IAM-89 | AC1 Markdown authoring in Proof field | IAM89-TC-01, IAM89-TC-02 |
| IAM-89 | AC2 View mode renders markdown accurately | IAM89-TC-03, IAM89-TC-04 |
| IAM-89 | AC3 Paste images into Proof editor | IAM89-TC-05, IAM89-TC-09 |
| IAM-89 | AC4 File upload mechanism exists | IAM89-TC-06 |
| IAM-89 | AC5 Images display clearly after save | IAM89-TC-07 |
| IAM-89 | AC6 Stored and retrieved consistently | IAM89-TC-08 |

## Detailed Test Cases

| ID | Ticket | AC | Category | Playwright | Description | Steps | Expected Result | Status |
|---|---|---|---|---|---|---|---|---|
| IAM88-TC-01 | IAM-88 | AC1 | Happy Path | Yes | Author a Work Log note with basic markdown toolbar and keyboard input | 1. Open TD-01. 2. Expand Work Log. 3. Enter TD-02 as author. 4. Enter markdown using toolbar and typing: bold, italic, unordered list, ordered list, link. 5. Click Log Work. | Entry saves successfully. Raw markdown is accepted in the editor without corruption. | ⏭ Skipped |
| IAM88-TC-02 | IAM-88 | AC1 | Edge | Yes | Save a Work Log note with rich mixed markdown and surrounding plain text | 1. Expand Work Log. 2. Enter TD-11 in the note editor. 3. Save the entry. | Saved content retains headings, quote, inline code, fenced code, link, and list syntax exactly once with no duplicated wrappers or missing characters. | ⏭ Skipped |
| IAM88-TC-03 | IAM-88 | AC2 | Happy Path | Yes | Verify Work Log viewer renders saved markdown in view mode | 1. Save a note from TC-01. 2. Observe the entry in the rendered log list. | Bold, italic, lists, and links render visually as formatted content. Raw markdown tokens are not shown in the viewer. | ⏭ Skipped |
| IAM88-TC-04 | IAM-88 | AC2 | Edge | Yes | Verify rendered Work Log handles rich markdown safely and completely | 1. Save TD-11 as a Work Log entry. 2. Collapse and re-expand Work Log if needed. 3. Inspect the rendered note. | Heading, quote, inline code, fenced code, and link render correctly. No unsafe HTML rendering appears. Layout remains readable. | ⏭ Skipped |
| IAM88-TC-05 | IAM-88 | AC3 | Happy Path | Yes | Paste a clipboard image into the Work Log markdown editor | 1. Copy TD-12 to the clipboard. 2. Focus the Work Log note editor. 3. Paste using the standard paste shortcut. 4. Wait for upload completion. 5. Save the entry. | Upload starts, temporary upload state appears, returned image markdown replaces the placeholder, and the image is included in the saved note. | ⏭ Skipped |
| IAM88-TC-06 | IAM-88 | AC4 | Happy Path | Yes | Upload an image to Work Log using the `Img` button | 1. Focus the Work Log note editor. 2. Click `Img`. 3. Select TD-04. 4. Wait for upload completion. 5. Save the entry. | The file picker opens, the selected file uploads successfully, and markdown for the image is inserted at the caret position. | ⏭ Skipped |
| IAM88-TC-07 | IAM-88 | AC5 | Compatibility | Partially | Validate all supported image formats in Work Log | 1. Repeat TC-06 with TD-04 through TD-08. 2. Save and inspect each result in view mode. | PNG, JPG, JPEG, GIF, and WEBP all upload successfully, render as images, and remain accessible after save. | ⏭ Skipped |
| IAM88-TC-08 | IAM-88 | AC6 | Persistence | Yes | Verify Work Log markdown and images persist after reload | 1. Save a Work Log note containing TD-11 plus one uploaded image. 2. Reload the page or reopen the ticket. 3. Expand Work Log. | The saved entry still shows full formatted markdown and the uploaded image. No raw placeholder or missing content appears after reload. | ⏭ Skipped |
| IAM88-TC-09 | IAM-88 | AC3, AC6 | Negative | Yes | Non-image clipboard paste should stay plain text in Work Log | 1. Copy TD-13 plain text. 2. Paste into the Work Log editor. 3. Save the note. | Plain text pastes normally, no upload is triggered, and saved content remains text-only. | ⏭ Skipped |
| IAM88-TC-10 | IAM-88 | AC4, AC5 | Negative | Yes | Unsupported file upload should not corrupt Work Log content | 1. Enter baseline markdown text in the Work Log editor. 2. Attempt to upload TD-09 through test automation or a manipulated file input. 3. Continue editing and save valid text. | Unsupported file is rejected or ignored, no broken image markdown is inserted, no stale uploading placeholder remains, and valid text still saves normally. | ⏭ Skipped |
| IAM89-TC-01 | IAM-89 | AC1 | Happy Path | Yes | Enter basic markdown in the Test Case Proof field | 1. Open TD-01. 2. Expand Test Cases. 3. Create or open TD-03. 4. Expand the row details. 5. Enter TD-10 markdown in Proof. 6. Blur the editor. | Proof accepts markdown input and stores it without stripping supported syntax. | ⏭ Skipped |
| IAM89-TC-02 | IAM-89 | AC1 | Edge | Yes | Enter rich markdown into Proof and verify null-to-text transitions | 1. Open a test case whose Proof is initially empty. 2. Enter TD-11 in Proof. 3. Blur the field. 4. Re-open the row. | Empty Proof becomes persisted markdown content. No literal `null`, empty placeholder residue, or truncated markdown appears. | ⏭ Skipped |
| IAM89-TC-03 | IAM-89 | AC2 | Happy Path | Yes | Verify Proof renders markdown accurately in view mode | 1. Save Proof content from TC-01. 2. Leave edit mode by clicking outside the Proof editor. 3. Observe the rendered Proof area. | Bold, italic, lists, and links render correctly in view mode. Raw markdown syntax is not displayed. | ⏭ Skipped |
| IAM89-TC-04 | IAM-89 | AC2 | Edge | Yes | Verify Proof render survives row collapse and re-expand | 1. Save TD-11 in Proof. 2. Collapse the test case row. 3. Re-expand it. 4. Inspect Proof display. | Rendered markdown remains intact after toggle. No state reset or fallback to raw syntax occurs. | ⏭ Skipped |
| IAM89-TC-05 | IAM-89 | AC3 | Happy Path | Yes | Paste a clipboard image into Proof | 1. Copy TD-12 to the clipboard. 2. Focus Proof. 3. Paste using the standard paste shortcut. 4. Wait for upload completion. 5. Blur the editor. | Pasted image uploads successfully, final markdown is inserted into Proof, and the saved Proof can render the image in view mode. | ⏭ Skipped |
| IAM89-TC-06 | IAM-89 | AC4 | Happy Path | Yes | Upload an image into Proof using the `Img` button | 1. Open Proof in edit mode. 2. Click `Img`. 3. Select TD-05. 4. Wait for upload completion. 5. Blur the editor. | File-picker upload succeeds and inserts image markdown into Proof at the intended position. | ⏭ Skipped |
| IAM89-TC-07 | IAM-89 | AC5 | Visual | Yes | Verify saved Proof images display at a usable size and remain clearly viewable | 1. Save a Proof containing one large and one small supported image. 2. Switch to view mode. 3. Inspect the rendered Proof visually at desktop width and a narrower viewport. | Images are visible, readable, and contained within the Proof area without breaking the row layout. Users can clearly understand the evidence shown. | ⏭ Skipped |
| IAM89-TC-08 | IAM-89 | AC6 | Persistence | Yes | Verify Proof markdown and images survive save, collapse, and reload | 1. Save Proof containing TD-11 and an uploaded image. 2. Collapse and re-expand the row. 3. Reload the page or reopen the ticket. 4. Re-open the same test case. | Proof content is stored and retrieved consistently. Markdown formatting and image references remain intact across all reloads. | ⏭ Skipped |
| IAM89-TC-09 | IAM-89 | AC3, AC6 | Negative | Yes | Plain-text paste in Proof must not trigger image upload behavior | 1. Copy TD-13 plain text. 2. Paste into Proof. 3. Blur the editor. | Plain text pastes normally, no image upload occurs, and resulting Proof content is saved as text only. | ⏭ Skipped |
| IAM89-TC-10 | IAM-89 | AC4, AC5, AC6 | Negative | Yes | Unsupported file upload should not corrupt Proof content or saved state | 1. Enter valid markdown into Proof. 2. Attempt to upload TD-09 through test automation or manipulated file input. 3. Blur and reopen the row. | Unsupported file is rejected or ignored, existing Proof content remains intact, no broken placeholder persists, and the row remains usable after reopening. | ⏭ Skipped |

## Cross-Feature Regression Focus

These checks are especially important because IAM-88 and IAM-89 share the same editor and renderer:

- Paste and file-picker image flows behave the same in Work Log and Proof
- Supported image formats behave consistently in both surfaces
- Non-image paste remains normal text paste in both surfaces
- Failed or unsupported uploads do not leave placeholder markdown behind
- Markdown render output is consistent between Work Log viewer and Proof view mode

## Playwright Notes

Recommended Playwright automation split:

- Automate IAM88-TC-01 through IAM88-TC-10 and IAM89-TC-01 through IAM89-TC-10 as UI flows
- Use file-upload automation for all supported-format checks
- Use clipboard injection for image-paste and plain-text-paste scenarios
- Capture at least one screenshot for rendered Work Log output and one for rendered Proof output after save
- Capture console messages after each upload scenario to detect silent client errors

## Execution Result

Execution date: 2026-04-17

Result: Blocked during environment setup.

Observed blocker:

- The UI dev server was reachable at `http://127.0.0.1:5173`.
- The backend required by the UI could not start successfully on port `8000`.
- Direct startup verification showed `database.init_db()` fails during Alembic upgrade with `alembic.util.exc.CommandError: Multiple head revisions are present for given argument 'head'`.
- Because the UI depends on backend-driven project, ticket, member, and event APIs, the app remained stuck on the loading state and returned `502 Bad Gateway` for requests such as `/projects`, `/tickets`, `/members`, and `/events`.
- No ticket detail modal could be opened, so the Work Log and Test Case Proof surfaces were not reachable for feature validation.

Evidence summary:

- Browser snapshot showed the app shell rendering with `Loading projects…` only.
- Browser console showed repeated `502 Bad Gateway` failures for backend-dependent endpoints.
- Backend init reproduced the root cause with: `uv run python -c "import asyncio; from database import init_db; asyncio.run(init_db())"`.

### Status Summary

| Outcome | Count |
|---|---:|
| PASS | 0 |
| FAIL | 0 |
| SKIP | 20 |
| MANUAL-PASS | 0 |

### Case Result Notes

All feature test cases were marked `SKIP` because the required ticket UI could not be reached. This was an environment blocker, not a feature-level assertion result.

## Exit Criteria

IAM-88 and IAM-89 are ready for QC sign-off when:

1. All acceptance criteria for both tickets are covered by executed test cases.
2. At least one successful clipboard paste and one successful file-picker upload are verified in both Work Log and Proof.
3. All supported image formats are verified successfully in at least one of the two feature surfaces and spot-checked in the other.
4. Persistence is confirmed after save and reload for both tickets.
5. Unsupported-input scenarios do not corrupt user content or leave the editor in a broken state.

## Bug Log

| Bug ID | Test Case | Description | Severity | Ticket |
|---|---|---|---|---|
| Untracked | ENV | Backend startup fails during `init_db()` because Alembic has multiple heads (`a1b2c3d4e5f6`, `e5f6a7b8c9d0`), preventing the UI from loading ticket data and blocking IAM-88/IAM-89 execution. | High | Not created |