---
title: "Test Plan: IAM-119 Blocker Re-run"
type: test
status: review
version: 1.0.1
created: 2026-04-22
updated: 2026-04-22
authors: [GitHub Copilot]
related:
  - docs/specs/IAM-100-idea-board-spec.md
---

# Test Plan: IAM-119 Blocker Re-run

## 1. Scope
Verify the two blockers previously reported for IAM-119 on branch `feat/IAM-100-3-idea-board` are fixed.

In scope:
- Server-side quick-create regression for `POST /projects/{project_id}/idea-tickets`
- UI drag-and-drop null-guard in `IdeaBoard.tsx`
- UI production build health

Out of scope:
- Full exploratory UI regression for the Idea Board
- Non-idea server behavior unrelated to IAM-119

## 2. Test Strategy

| Layer | Approach | Tools |
|---|---|---|
| API regression | Run idea-focused server pytest selection | pytest |
| Code verification | Review the drag-end guard in `IdeaBoard.tsx` | source review |
| Build regression | Run production UI build | npm |

## 3. Test Cases

| ID | Description | Preconditions | Steps | Expected Result |
|---|---|---|---|---|
| TC-01 | Quick-create route no longer fails with 405 | Server virtualenv available | Run `cd server && source .venv/bin/activate && pytest tests/ -v -k "idea"` | Idea-related server tests pass, including coverage for idea create route |
| TC-02 | Drag-and-drop handles missing `ideaStatus` safely | Branch checked out locally | Review `handleDragEnd` in `ui/src/components/IdeaBoard.tsx` | Code defaults `ticket.ideaStatus` to `draft` before transition guard |
| TC-03 | UI still builds after blocker fixes | UI dependencies installed | Run `cd ui && npm run build` | Build exits successfully with no new errors |

## 4. Coverage Goals

| Area | Target |
|---|---|
| Previously reported blockers | 100% re-verified |
| Adjacent regression signal | No new failures in requested checks |

## 5. Execution Result
- TC-01: Not confirmed
  - Backend startup command `cd server && source .venv/bin/activate && python main.py` failed immediately with `ImportError: cannot import name 'create_idea_ticket' from 'services.tickets'`.
  - Requested regression command `cd server && source .venv/bin/activate && pytest tests/ -v -k "idea" 2>&1 | tail -30` exited with code `2` during collection.
  - Reported collection failures:
    - `tests/test_main.py`
    - `tests/test_projects.py`
    - `tests/test_tickets.py`
  - Because the suite aborted before executing idea-route assertions, the quick-create 405 fix is not verified by this rerun.
- TC-02: Pass
  - `ui/src/components/IdeaBoard.tsx` defaults `ticket.ideaStatus ?? 'draft'` before the transition `has()` check inside `handleDragEnd`.
  - This addresses the prior crash path where an undefined status reached `ALLOWED_TRANSITIONS[currentStatus]`.
- TC-03: Pass
  - Requested build check `cd ui && npm run build 2>&1 | tail -10` exited with code `0`.
  - Build completed successfully with only the existing bundle-size warning for the main JS chunk.