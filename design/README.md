# Design

Redesign workspace for the Kanban Board app (React UI in `ui/`).

- **Canvas:** https://claude.ai/artifact/B3jiFjnoEL9erRN7QYRHj2 — live design canvas (artboards for each screen/component), currently empty.
- **Screens to cover:** board view, ticket detail modal, list view, and any shared UI (nav, filters, forms).

Artboards get added to the canvas as each screen is redesigned; this folder holds any exported specs, notes, or assets that come out of that process.
